# Application Configuration
# Required - Application name used throughout the frontend
VITE_APP_NAME=CodeQuest
# Required - Application version for asset caching and version display
VITE_APP_VERSION=1.0.0

# API Configuration
# Required - Backend API base URL for all API requests
VITE_API_URL=http://localhost:3000
# Required - API version for versioned endpoint calls
VITE_API_VERSION=v1
# Optional - API request timeout in milliseconds (1000-60000)
VITE_API_TIMEOUT=30000
# Optional - Maximum number of API retry attempts (0-5)
VITE_MAX_RETRIES=3
# Optional - Delay between retry attempts in milliseconds (100-5000)
VITE_RETRY_DELAY=1000

# WebSocket Configuration
# Required - WebSocket server URL for real-time updates
VITE_WEBSOCKET_URL=ws://localhost:3001

# Authentication Configuration
# Required - Authentication domain for SSO integration
VITE_AUTH_DOMAIN=auth.codequest.com
# Required - OAuth client ID for authentication (sensitive)
VITE_AUTH_CLIENT_ID=your-client-id
# Required - OAuth redirect URI after authentication
VITE_AUTH_REDIRECT_URI=http://localhost:5173/callback

# Multi-tenant Configuration
# Required - HTTP header name for multi-tenant identification
VITE_TENANT_HEADER=X-Tenant-ID

# Security Configuration
# Optional - Enable CSRF protection for API requests
VITE_ENABLE_CSRF=true
# Optional - HTTP header name for CSRF token
VITE_CSRF_HEADER=X-CSRF-Token
# Required - Enable secure and HttpOnly cookies
VITE_SECURE_COOKIES=true
# Required - Allowed CORS origins (comma-separated)
VITE_CORS_ORIGINS=*

# Monitoring & Analytics Configuration
# Optional - Flag to enable/disable analytics tracking
VITE_ENABLE_ANALYTICS=false
# Optional - Enable metrics collection for monitoring
VITE_METRICS_ENABLED=true
# Optional - Application logging level (debug, info, warn, error)
VITE_LOG_LEVEL=info
# Optional - Response time SLA threshold in milliseconds (100-2000)
VITE_RESPONSE_SLA=500

# Theme Configuration
# Optional - Default theme mode for the application (light/dark)
VITE_DEFAULT_THEME=light

# Production Values (uncomment and update for production)
# VITE_API_URL=https://api.codequest.com
# VITE_WEBSOCKET_URL=wss://ws.codequest.com
# VITE_AUTH_REDIRECT_URI=https://app.codequest.com/callback
# VITE_CORS_ORIGINS=https://app.codequest.com,https://admin.codequest.com
# VITE_SECURE_COOKIES=true
# VITE_ENABLE_CSRF=true
# VITE_LOG_LEVEL=error